# v0 sandbox internal files
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts
.snowflake/
.v0-trash/
.vercel/

# Environment variables
.env*.local

# Common ignores
node_modules
.next/
.DS_Store

# Runtime parser config & secrets — per-deployment, never commit.
# Committing these caused a dead proxy (mproxy.site) to keep returning on
# every `git pull` and made the rotating cookies file block pulls.
storage/avito_proxy.txt
storage/avito_proxy_change_url.txt
storage/avito_cookies.json
